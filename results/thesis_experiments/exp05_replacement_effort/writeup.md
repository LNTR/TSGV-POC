The replacement task was limited to the internal encoder seam in `visual-feature-extraction-service`. Support for `resnet18` was added behind the existing `encoder` request field, and no OpenAPI contract file was changed. In this scenario, `resnet18` served as a replacement candidate to test internal backbone swapability without changing the service contract; it was not used to argue that `resnet18` improves grounding accuracy. The scoped diff touched `2` files with `47` added lines and `9` removed lines, and the measured time on task was `18.1` minutes.

Validation was run against the live TGN stack. A `resnet18` preprocess request succeeded with the existing `/jobs/preprocess` path, the replacement smoke completed `process-aligned-text`, training, inference, and evaluation, and the training metadata reported `visual_feature_size` `512`. The pre-change and post-change guard evaluations both returned `R@1_IOU0.5` of `0.0`, so no new accuracy claim was made from this smoke-sized check.
