The Kubernetes autoscaling experiment was deferred on this host. `kubectl` was not installed, so HPA deployment, `kubectl top`, and replica-timing measurements could not be executed without guessing.

All quantitative scaling fields were left as `not_run`. No autoscaling or resource-efficiency claim was made from this machine.
