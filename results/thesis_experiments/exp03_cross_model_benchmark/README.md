Run context: 2026-04-18 on host `ucsc-ThinkPad-T470p` (`Linux 6.17.0-20-generic`) with GPU `NVIDIA GeForce 940MX`. This remained a TGN-only smoke pass rather than a full matched benchmark run. The checkout now contains the prior `Results/compose_dataset_60_20_20/` folder, so the previously recorded TGN split files, scores, and phase timings could be recovered from that artifact even though no fresh rerun was performed in this pass.
