The reuse inventory recorded `9` concrete shared artifacts in the TGN-only scope. The six OpenAPI contracts were each consumed by one TGN service implementation, and `service_registry.py` was imported by `6` TGN service entrypoints. The shared routing layer remained present as one operator implementation and one `ModelRouter` CRD referenced by the Kubernetes manifests and examples.

Runtime reuse was also observed for repeated requests. A repeated preprocess request on `s27-d34` dropped from `25.701` seconds to `0.105` seconds, and both preprocess and forwarded feature metadata changed from cache misses to cache hits on the second request. A repeated text-processing request on the same logical input dropped from `0.104` seconds to `0.007` seconds and also changed from a cache miss to a cache hit. No cross-model reuse claim was made in this TGN-only scope.
