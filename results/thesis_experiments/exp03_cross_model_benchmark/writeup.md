The prior `Results/compose_dataset_60_20_20/` artifact is present in this checkout, so the split files and previously recorded TGN benchmark outputs could be recovered from that folder. `summary.json` records the recovered TGN scores as `R@1_IOU0.5 = 0.0` and `R@3_IOU0.5 = 0.0`, with prior TGN phase timings of `8218.184` seconds for preprocessing, `260.132` seconds for text processing, `286.123` seconds for training, `0.59` seconds for inference, and `316.266` seconds for evaluation. The split fingerprint recovered from `train.json`, `val.json`, and `test.json` is `sha256:69b39fef1e037686f45ffd2a41f1ed619fb8c0ac3608244ad2dc8350872956b0`.

A full matched benchmark was still not run in this pass, so the experiment remains partial rather than complete. `summary.json` records `2` successful TGN smoke checks: `TGN` text-processing returned `GET /registry/self`, accepted `1` aligned-text request against `dataset/texts/s27-d34.aligned.tsv`, and wrote `shared://text/processed/smoke-s27-d34-e1.tp.json`. `TALL` was not included in the follow-up scope and its benchmark fields remain `"not_run"`.
