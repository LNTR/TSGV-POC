The monolithic baseline experiment was not run in this pass. No upstream monolithic TGN checkout was present in the repository, and the prompt did not provide a pinned upstream URL, commit, or tag to use for a comparable baseline run.

All requested measurement fields were therefore recorded as `"not_run"` in `summary.json`. No wall-clock, memory, or setup-effort estimate was reported without a real baseline execution.
